_schema-version: "3.2"
ID: appflightdetail
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appflightdetail-destination-content
  type: com.sap.application.content
  requires:
  - name: appflightdetail-destination-service
    parameters:
      content-target: true
  - name: appflightdetail-repo-host
    parameters:
      service-key:
        name: appflightdetail-repo-host-key
  - name: appflightdetail-uaa
    parameters:
      service-key:
        name: appflightdetail-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appflightdetail_html_repo_host
          ServiceInstanceName: appflightdetail-html5-service
          ServiceKeyName: appflightdetail-repo-host-key
          sap.cloud.service: appflightdetail
        - Authentication: OAuth2UserTokenExchange
          Name: appflightdetail_uaa
          ServiceInstanceName: appflightdetail-xsuaa-service
          ServiceKeyName: appflightdetail-uaa-key
          sap.cloud.service: appflightdetail
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appflightdetail-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appflightdetail-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appflightdetail.zip
      name: appflightdetail
      target-path: resources/
- name: appflightdetail
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appflightdetail-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appflightdetail-destination-service
    service-plan: lite
- name: appflightdetail-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appflightdetail-xsuaa-service
    service-plan: application
- name: appflightdetail-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appflightdetail-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
